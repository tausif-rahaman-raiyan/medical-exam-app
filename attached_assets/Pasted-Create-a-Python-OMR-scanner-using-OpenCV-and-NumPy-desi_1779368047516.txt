Create a Python OMR scanner using OpenCV and NumPy designed for this specific 100-question medical admission sheet.
1. Image Preprocessing:
Detect the four black corner squares to perform a perspective transform, warping the image to a fixed size of 1000x1400 pixels.
Use Gaussian blur and adaptive thresholding to isolate the black bubbles from the white background.
2. Coordinate Mapping Logic:
The sheet is organized into 4 main columns of 25 questions each. Define the grid as follows (based on a 1000x1400 scale):
Grid Start: Y-coordinate starts at 480 and ends at 1350.
Column X-Offsets (Question Numbers):
Col 1 (Q 1-25): x=85
Col 2 (Q 26-50): x=315
Col 3 (Q 51-75): x=545
Col 4 (Q 76-100): x=775
Bubble Offsets: Within each column, the 4 bubbles (A, B, C, D) are spaced 35 pixels apart horizontally.
Row Spacing: Each of the 25 rows is spaced 34.5 pixels apart vertically.
3. Detection Logic:
For each coordinate, crop a small circular mask.
Count non-zero (white) pixels in the thresholded image.
If a bubble has a significantly higher pixel count than others in its row, mark that index (0, 1, 2, or 3) as the answer.
4. Output:
Return a dictionary mapping questions 1-100 to detected answers.
Provide a simple Flask or Streamlit UI to upload the image and see the results overlaid on the original image for verification.
Technical Breakdown of the Mapping Logic
If you are writing the code manually or tweaking what the AI gives you, here is the math used to identify the 
(
x
,
y
)
(x,y)
 of any bubble on this specific sheet:
1. Vertical Position (
y
y
)
Since there are 25 rows, we calculate the 
y
y
 center for any question 
n
n
 (where 
n
n
 is 1 to 25):
y
=
TopMargin
+
(
(
n
−
1
)
×
RowHeight
)
y=TopMargin+((n−1)×RowHeight)

Using the image provided: 
y
=
480
+
(
(
n
−
1
)
×
34.5
)
y=480+((n−1)×34.5)
2. Horizontal Position (
x
x
)
First, identify which column block the question falls into:
Block 1 (1-25): Base 
x
=
135
x=135
Block 2 (26-50): Base 
x
=
365
x=365
Block 3 (51-75): Base 
x
=
595
x=595
Block 4 (76-100): Base 
x
=
825
x=825
Then, add the offset for the specific bubble (
i
i
 where A=0, B=1, C=2, D=3):
x
=
BlockBase
+
(
i
×
35
)
x=BlockBase+(i×35)
Why this works for this specific image:
The Timing Marks: The red dashes on the left and right are used by professional scanners. In your code, the Corner Squares (Top-Left, Top-Right, etc.) are better because they allow the code to "fix" a photo taken at an angle.
QR Code/Barcode: Your code should ignore the top 30% of the image (the area above 
y
=
450
y=450
) during the bubble-scanning phase to avoid false positives from the Barcode or QR code.
Density Check: Because these bubbles are quite small, the AI needs to use a "Mask" method—it compares the four bubbles of one question against each other. Whichever one is the darkest is the answer.