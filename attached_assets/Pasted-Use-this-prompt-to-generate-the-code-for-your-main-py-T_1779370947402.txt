Use this prompt to generate the code for your main.py. This prompt forces the AI to build a geometry-aware system rather than a "hard-coded" one:"I have an OMR sheet with black registration marks at the four corners. Write a Python function for me using OpenCV that:Detects the four corner markers using cv2.findContours and filters them by area/aspect ratio to ensure they are the registration marks.Uses cv2.getPerspectiveTransform to warp the image based on these four points.Calculates the distance between the top-left and top-right markers to determine the current image scale.Dynamically scales my bubble_coordinates.json (which is based on a standard 1000x1500 pixel template) to match the dimensions of the warped input image.Ensure the coordinate scaling uses a ratio factor: factor = current_width / template_width."The Logic Behind the PromptBy asking for Dynamic Scaling (step 4), you are essentially telling the code: "Don't look for the bubble at pixel X=100. Look for the bubble at (100 * scale_factor)." This is what makes your OMR reader 100% accurate regardless of how close or far the camera is held.Implementation Checklist for ReplitTo make sure this works in your environment, organize your files as follows:template_coords.json: This file should contain the coordinates of your bubbles as percentages or a fixed base-resolution (e.g., all coordinates based on a 1000x1400 canvas).main.py:Step 1: Load image.Step 2: Find corners $\rightarrow$ Calculate Warp Matrix.Step 3: Calculate scale_factor = (Detected_Width) / (Base_Width_of_Template).Step 4: Iterate through the JSON and multiply each (x, y) by the scale_factor.Step 5: Crop and perform the "Relative Pixel Counting" (the density check I provided earlier).A Note on the "100% Accuracy" GoalIf you want to achieve high reliability, avoid relying on the QR code for everything. The QR code is great for identifying which version of the test it is, but the four corner markers are much more reliable for geometric alignment